#pragma once
#include <SFML/Graphics.hpp>
class StaticNumber
{
public:
	static const int N, M, size, wWindow, hWindow; //size: kích thước của các phần tử (snake, food, zone) trong cửa sổ playgame
	static int MODE, SPEED;
};

